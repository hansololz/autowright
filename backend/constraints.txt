# Pinned runtime dependency closure for the §3 distributable build (SPEC §17).
#
# pyproject.toml declares floors (>=) so the dev venv can move forward; this
# file is what makes a release reproducible. `scripts/prod.sh` installs the
# backend into the bundled interpreter with `pip install -c constraints.txt`,
# so two DMGs cut from one commit ship byte-identical dependencies. That
# includes the §6.2 curated packages, which are part of the user-facing step
# environment: a silent lxml/requests bump changes what step code sees.
#
# Direct AND transitive: a constraints file only pins what pip decides to
# install, so anything left out is free to float.
#
# Maintenance: after intentionally moving a floor in pyproject.toml, refresh
# these pins from the dev venv (`.venv/bin/pip freeze`, runtime closure only:
# no dev extras, no editable autowright) and commit the two together.

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
beautifulsoup4==4.15.0
certifi==2026.7.22
# Linux only: keyring's freedesktop Secret Service backend (secretstorage +
# jeepney, plus secretstorage's cryptography closure) — without these keyring
# silently resolves the null backend and every secret read answers nothing;
# without the pins a Linux build would resolve them unconstrained (SPEC §17
# marker rule).
cffi==2.1.1; sys_platform == "linux"
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1; sys_platform == "linux"
fastapi==0.141.1
feedparser==6.0.14
feedparser-sgmllib==2.1.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.20
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
jeepney==0.9.0; sys_platform == "linux"
keyring==25.7.0
lxml==6.1.3
more-itertools==11.1.0
packaging==26.3
pycparser==3.0; sys_platform == "linux"
pydantic==2.13.5
pydantic-core==2.46.5
python-dateutil==2.9.0.post0
# Windows only: keyring's Credential Locker backend needs it; without the pin
# a Windows build would resolve it unconstrained (SPEC §17 marker rule).
pywin32-ctypes==0.2.3; sys_platform == "win32"
PyYAML==6.0.3
requests==2.34.2
secretstorage==3.5.0; sys_platform == "linux"
six==1.17.0
soupsieve==2.9.2
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
# Windows only: no system IANA tz database exists there (python-build-standalone
# ships none either) — zoneinfo needs this or no timezone resolves (SPEC §17).
tzdata==2026.4; sys_platform == "win32"
urllib3==2.8.0
uvicorn==0.53.0
websockets==17.1
